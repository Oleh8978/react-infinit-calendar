import { StyleSheet } from 'react-native';
import colors from '../../../../styles/colors';

export default StyleSheet.create({
  logo: {
    position: 'absolute',
    left: 20,
  },
  signUpText: {
    color: colors.black,
    fontSize: 18,
  },
});
